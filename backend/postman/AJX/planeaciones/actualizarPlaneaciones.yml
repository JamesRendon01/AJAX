info:
  name: actualizarPlaneaciones
  type: http
  seq: 4

http:
  method: PUT
  url: http://127.0.0.1:8000/planeaciones/2
  body:
    type: json
    data: |-
      {
          "nombre": "Planeacion marzo"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
