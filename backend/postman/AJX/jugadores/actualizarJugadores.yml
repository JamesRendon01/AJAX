info:
  name: actualizarJugadores
  type: http
  seq: 4

http:
  method: PUT
  url: http://127.0.0.1:8000/jugadores/2
  body:
    type: json
    data: |-
      {
          "nacionalidad": "colombiano"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
