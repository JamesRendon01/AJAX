info:
  name: actualizarCategorias
  type: http
  seq: 4

http:
  method: PUT
  url: http://127.0.0.1:8000/categorias/2
  body:
    type: json
    data: |-
      {
          "nombre": "mayores"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
